<div>
	<%= file_field_tag "post[photos][#{picname}]", :class => "uploadify", :picname => picname %>
	<div id="<%= "#{picname}_remover" %>">
		<%= link_to("remove", destroy_photo_posts_path(@images.first.slot, @_action), :class => "remove_link", :picname => "#{picname}") if @images.present? %>
	</div>
</div>